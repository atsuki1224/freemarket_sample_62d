-# =====人気のカテゴリー=============================
.new-item
  .new-item__area
    .item-header
      .item-header__title
        レディース新着アイテム
      .item-header__view-more
        もっと見る＞
    .item-index
      - @ladies_products.each do |ladies_product|
        -product_image = ladies_product.images.first
        .contents
          =link_to product_path(ladies_product) do
            .contents__img{style: "background-image: url(#{product_image.image});"}
              -if ladies_product.trade_status != 0
                .sold_out
                  =image_tag "icons/sold_out.png",class:"sold_out"
              .contents__price
                = "¥,#{ladies_product.price}"
            .contents__text
              = ladies_product.item_name
    .item-header
      .item-header__title
        メンズ新着アイテム
      .item-header__view-more
        もっと見る＞
    .item-index
      - @mens_products.each do |mens_product|
        -product_image = mens_product.images.first
        .contents
          =link_to product_path(mens_product) do
            .contents__img{style: "background-image: url(#{product_image.image});"}
              -if mens_product.trade_status != 0
                .sold_out
                  =image_tag "icons/sold_out.png",class:"sold_out"
              .contents__price
                = "¥,#{mens_product.price}"
            .contents__text
              = mens_product.item_name

    .item-header
      .item-header__title
        家電・スマホ・カメラ新着アイテム
      .item-header__view-more
        もっと見る＞
    .item-index
      - @home_appliance_products.each do |home_appliance_product|
        -product_image = home_appliance_product.images.first
        .contents
          =link_to product_path(home_appliance_product)do
            .contents__img{style: "background-image: url(#{product_image.image});"}
              -if home_appliance_product.trade_status != 0
                .sold_out
                  =image_tag "icons/sold_out.png",class:"sold_out"
              .contents__price
                = "¥,#{home_appliance_product.price}"
            .contents__text
              = home_appliance_product.item_name

    .item-header
      .item-header__title
        おもちゃ・ホビー・グッズ新着アイテム
      .item-header__view-more
        もっと見る＞
    .item-index
      - @toy_products.each do |toy_product|
        -product_image = toy_product.images.first
        .contents
          =link_to product_path(toy_product) do
            .contents__img{style: "background-image: url(#{product_image.image});"}
              -if toy_product.trade_status != 0
                .sold_out
                  =image_tag "icons/sold_out.png",class:"sold_out"
              .contents__price
                = "¥,#{toy_product.price}"
            .contents__text
              = toy_product.item_name